# Dependencies
node_modules/

# Next.js build output
.next/
out/

# TypeScript incremental build info
*.tsbuildinfo

# Regenerated by Next.js on every build; was never tracked here
next-env.d.ts

# Environment variables — never commit real keys
.env
.env.local
.env*.local

# OS / editor noise
.DS_Store
Thumbs.db
